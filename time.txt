package main

import (
	"fmt"
	"time"
)

func main() {
	now := time.Now()

	fmt.Println("now=>", now)
	//时间戳
	fmt.Println("unix=>", now.Unix())
	fmt.Println("unixNano=>", now.UnixNano())

	var t1 int
	t1 = int(now.Unix()) //return int64
	fmt.Println(t1)
}
